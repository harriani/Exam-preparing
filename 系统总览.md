# 国缆杯学习/考试系统 v2 — 系统总览

> 一句话：**理解/出题/材料全交大模型 + 技能（产出数据），判分/出卷/学习全用脚本确定性消费（不调大模型）。**
> 代码只许出现在 `engine/`（判分 + 薄渲染），其余全是数据。

## 框架分层

| 层 | 谁干 | 产出 | 落盘位置 | 是否已就位 |
|---|---|---|---|---|
| L1 抽取 | `doc2kb` 技能 | 文本块（纯提取，无判断） | `kb/raw/pages_*.json` | ✅ 2951.13/21/31/32 |
| L2 理解 | **LLM（我）** | 结构化 KU（知识点） | `kb/judged.json` | ✅ 13 条考点 |
| L3a 题库 | **LLM + standards-exam-generator 技能** | 选择/判断/填空/计算 | `rules/*.json` | ✅ 2951 全、11/12 |
| L3b 材料 | **LLM（我）** | 闪卡/参数表/自测/易错点 | `materials/*.json` | ✅ 2951 家族首份 |
| L4 判分 | 脚本死规则（双实现） | 分数 | `engine/generate.py` + `engine/grade.js` | ✅ parity 13/13 |
| L5 出卷 | 按你给的权重从题库抽题 | 试卷 | `out/*_exam.html` | ✅ 渲染壳 |
| L6 学习 | 确定性消费材料 | 学习页 | `out/*_study.html` | ✅ 渲染壳 |

## 资产登记表（截至 2026-08-04）

| 标准 | 标准库(KU) | 题库 | 学习材料 | 状态 |
|---|---|---|---|---|
| GB/T 2951.13/21/31/32 | ✅ 13 条 | ✅ `gbt_2951_kb_mvp.json`(43题) | ✅ `gbt_2951_materials.json` | **可用** |
| GB/T 2951.11/12 | — | ✅ `gbt_2951_11_12.json` | ⏳ 待补 KU 后生成 | 题库有，材料缺 |
| GB/T 3956 | — | ⚠️ `gbt_3956.json` | ⚠️ 仅生抽取文本 | **质量差·待重下**，不信任 |

> ⚠️ **3956 是水印垃圾**，其 `rules/gbt_3956.json` 和 `materials/gbt_3956_extracted.txt` 不可信，等你去重下干净版后再建 KU/题库/材料。

## 三库（系统核心资产）

1. **标准库** = `kb/judged.json`（LLM 理解后的知识点，带 src 溯源）
2. **题库** = `rules/*.json`（LLM 出题，全字段 `src` 溯源，零幻觉）
3. **学习材料库** = `materials/*.json`（L3b，闪卡/参数表/自测/易错点）

三库关系：**同一份 KU 派生出题库和材料**，口径天然一致；下游出卷/学习只读取，不调大模型。

## 怎么往系统里加新文件

**直接把文件丢对话框里**说"处理这份"即可（PDF/Word/扫描件都行）。流程：
`doc2kb抽 → 我编KU → 我出题库+材料 → 落 kb/rules/materials → 渲染出卷/学习页`。

也可先把文件放 `inbox/`，再让我处理。详见 `inbox/README.md`。

## 红线（踩过坑后立的）

- ❌ **不写任何"生成/理解/出题"脚本** —— 那是被淘汰的 25+ `_gen_gbXX.py` 补丁思路。
- ✅ 出题"真源"= 大模型依据 KU 直接产出数据，不是某个 `.py` 的规则。
- ❌ 旧项目 `question_bank.py`(54KB)/`generator.py`(90KB) 整搬 = 把债搬过来，只学"五闸/去重/质量闸"思想，不搬代码。
- ⚠️ 闭卷判分必须走服务端 `engine/generate.py`，HTML 内联答案仅供自测。
- 🔑 配 LLM key 时守"公共机不配密钥"铁律，只在你本机环境配。
