# v2 最小 MVP 跑通说明（2026-08-04）

## 架构落地：大模型出题 + 脚本判题

```
干净 PDF ──Docling OCR──> 原始文本 ──大模型判定──> 考点 KU (judged.json)
                                                        │
                                          大模型出题（本轮由我完成）
                                                        ↓
                                            rules/*.json（题库数据）
                                                        │
                            ┌───────────────────────────┴──────────────┐
                     engine/generate.py                        engine/grade.js
                     （Python 判分·权威）        ←镜像→        （浏览器判分·离线）
                            │                                          │
                     满分/零分双向自检                        可作答试卷 HTML
                            └──────── test_parity.py 保证两边分数完全一致 ────────┘
```

**大模型只碰"出题"一步，判分 100% 是死规则脚本。**

## 本次产出

| 文件 | 作用 |
|---|---|
| `rules/gbt_2951_kb_mvp.json` | 100 分题库数据（43 题，全部溯源到标准条款） |
| `engine/generate.py` | 判分内核（Python，权威）+ 校验 + 双向自检 + 答案预览 |
| `engine/grade.js` | 判分内核（JS，逐条镜像 Python），供浏览器离线判分 |
| `engine/render_exam.py` | 生成可作答试卷 HTML |
| `engine/test_parity.py` | Python↔JS 判分一致性测试（13 组用例） |
| `out/gbt_2951_kb_mvp_exam.html` | **可作答 + 自动判分的离线试卷（49KB，双击即用）** |
| `out/gbt_2951_kb_mvp_preview.html` | 带答案的教师版预览 |

## 试卷构成（100 分 / 43 题）

| 题型 | 题量 | 分值 | 判分规则 |
|---|---|---|---|
| 单项选择 | 15 | 30 | 选项索引精确匹配 |
| 判断 | 10 | 10 | 支持 对/√/正确/true/1 等 7 种写法 |
| 填空 | 15 | 30 | 归一化匹配 + 近义词表；**整题全对才给分** |
| 计算与判定 | 3 | 30 | 数值容差判分；合格性判定走文本匹配 |

覆盖 GB/T 2951.13 / .21 / .31 / .32 四份标准的 13 条考点 KU。

## 引擎这次的改动

1. **新增判断题（judge）题型** —— 原引擎只有选择/填空/连线/计算，缺你最常用的判断题。
2. **新增零分反向自检** —— 故意全答错必须得 0 分，防止判分逻辑写反（原来只有满分自检）。
3. **收紧数值解析** —— 原来用 `float()`，会把 `1_0` 当 10、接受 `inf`/`nan`；改为严格正则，只认纯数字串。
4. **归一化补全** —— 增加全角空格 U+3000、制表符的清除。
5. **新增 src 溯源字段校验** —— 每题必须标注标准号+条款，缺失会告警。

## 验证结果

```
满分自检 PASS (100/100)     零分自检 PASS (全答错=0)
PARITY   PASS  13/13 组用例 Python 与 JS 判分完全一致
  含：满分卷 / 全错卷 / 全空卷 / 近义词+空格噪声卷 / 判断题多写法卷
      判断题乱填卷 / 数值毒性输入卷 / 计算题容差边界卷 / 随机卷×5
HTML 结构  48 个输入框 = 31 填空位 + 17 计算小问，80 个单选，0 外链依赖
```

**数值毒性输入卷**是重点：`1_0`、`inf`、`nan`、`4h`、`0.6abc` 这类输入，Python 的 `float()`
和 JS 的 `parseFloat()` 天然行为不同，不处理就会出现"网页判 100 分、后台判 80 分"。现已两边统一判 0。

## 加一份新标准要做什么

只写一个 `rules/<标准>.json`，引擎代码一行不改：

```bash
python engine/generate.py   rules/新标准.json    # 校验 + 双向自检 + 教师版预览
python engine/render_exam.py rules/新标准.json    # 生成可作答试卷
python engine/test_parity.py rules/新标准.json    # 验证判分一致性
```

## 已知边界

- 试卷 HTML 为**离线自测卷**，答案内联在页面里（查看源码可见）。闭卷考试须走服务端判分
  （调 `generate.py` 的 `grade_all`），不能用这个文件。
- 填空题整题全对才给分，多空题偏严。若要按空给分，改 `grade_fill` 与 `gradeFill` 两处（必须同步）。
- 目前题库只覆盖 4 份标准 / 13 条 KU。其余标准需先重下干净 PDF → OCR → 大模型判考点。
